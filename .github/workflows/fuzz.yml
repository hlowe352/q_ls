name: nightly fuzz

on:
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:

jobs:
  fuzz:
    name: parse target (10 min)
    uses: ./.github/workflows/_fuzz.yml
    with:
      max_total_time: 600
      artifact_name: fuzz-artifacts-nightly
